info:
  name: auth
  type: folder
  seq: 1

request:
  auth: inherit
  scripts:
    - type: tests
      code: |-
        const token = res.body.token;
        bru.setEnvVar("jwt", token);

        console.log(
        bru.getEnvVar("jwt"))
