info:
  name: delete-room
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/admin/rooms/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
