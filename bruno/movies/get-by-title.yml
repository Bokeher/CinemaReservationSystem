info:
  name: get-by-title
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/movies?title=interstellar"
  params:
    - name: title
      value: interstellar
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
