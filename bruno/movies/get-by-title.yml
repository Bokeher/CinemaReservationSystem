info:
  name: get-by-title
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/movies?title=shrek"
  params:
    - name: title
      value: shrek
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
