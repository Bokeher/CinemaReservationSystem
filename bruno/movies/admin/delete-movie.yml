info:
  name: delete-movie
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/admin/movies/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
