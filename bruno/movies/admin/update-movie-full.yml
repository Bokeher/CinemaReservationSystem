info:
  name: update-movie-full
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/admin/movies/3"
  body:
    type: json
    data: |-
      {
        "title": "Shrek 2",
        "description": "Shrek again.",
        "requiredAge": 7,
        "durationMinutes": 90
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
