info:
  name: confirm-reservation
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/reservations/2/confirm"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
