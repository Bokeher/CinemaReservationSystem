info:
  name: cancel-reservation
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/reservations/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
