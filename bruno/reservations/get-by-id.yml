info:
  name: get-by-id
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/reservations/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
