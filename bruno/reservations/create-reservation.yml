info:
  name: create-reservation
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/reservations"
  body:
    type: json
    data: |-
      {
        "screeningId": 1,
        "seatId": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
