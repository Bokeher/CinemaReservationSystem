info:
  name: get-all-for-current-user
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/reservations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
