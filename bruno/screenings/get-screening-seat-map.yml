info:
  name: get-screening-seat-map
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/screenings/1/seat-map"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
