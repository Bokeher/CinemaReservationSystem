info:
  name: update-screening
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/admin/screenings/1"
  body:
    type: json
    data: |-
      {
        "movieId": 1,
        "roomId": 1,
        "startTime": "2026-05-20T18:00:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
