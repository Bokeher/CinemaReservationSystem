info:
  name: create-screening
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/admin/screenings"
  body:
    type: json
    data: |-
      {
        "movieId": 1,
        "roomId": 1,
        "startTime": "2026-05-20T18:30:00"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
