info:
  name: delete-screening
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/admin/screenings/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
