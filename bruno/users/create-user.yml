info:
  name: create-user
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/users
  body:
    type: json
    data: |-
      {
        "username": "test",
        "email": "ab@b.com",
        "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
