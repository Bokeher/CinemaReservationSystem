info:
  name: get-by-username
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/users?username=test
  params:
    - name: username
      value: test
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
