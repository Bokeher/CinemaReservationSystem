info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/users/login
  body:
    type: json
    data: |-
      {
        "username": "test",
        "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
