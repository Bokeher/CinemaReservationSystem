info:
  name: update-user-partial
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/admin/users/2"
  body:
    type: json
    data: |-
      {
        "username": "user5"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
