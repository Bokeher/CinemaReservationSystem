info:
  name: update-user-full
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/admin/users/2"
  body:
    type: json
    data: |-
      {
        "username": "user",
        "email": "user@b.com",
        "password": "user1234",
        "role": "USER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
