info:
  name: get-by-username
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/users?username=user"
  params:
    - name: username
      value: user
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
