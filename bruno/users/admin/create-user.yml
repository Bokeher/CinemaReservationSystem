info:
  name: create-user
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/admin/users"
  body:
    type: json
    data: |-
      {
        "username": "user",
        "email": "user@b.com",
        "password": "user",
        "role": "USER"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
